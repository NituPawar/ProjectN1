git is a tool used to manipulate changes from local repo to gitup repo

//git commands 
if u are not known then use this
git config -global user.email("email")
git config -global user.name("username")

Basic commands using git bash
git init //initiallize the repository 
git remote add origin (url of repo) // establishes connection
git  add ./(file name) // for adding file to githup repo
git push origin branchname
git pull (url)
git clone (url)
git show
git status
git log
git merge
git branch (branch_name)
git branch (head point)
git checkout another-branchname
git rm filename/folder
git reset //resets unstaged changes

